set nocompatible
filetype plugin on
syntax on
set linebreak

let g:vimwiki_list = [{'path': '~/Documents/work/vimwiki', 'syntax': 'default', 'ext': '.md'}]

call plug#begin()

Plug 'vimwiki/vimwiki'

call plug#end()
